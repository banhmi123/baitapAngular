import { HightLinghtDirective } from './hight-linght.directive';

describe('HightLinghtDirective', () => {
  it('should create an instance', () => {
    const directive = new HightLinghtDirective();
    expect(directive).toBeTruthy();
  });
});
